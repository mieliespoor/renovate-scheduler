{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "renovate-scheduler.serviceAccountName" . }}
  labels:
    {{- include "renovate-scheduler.labels" . | nindent 4 }}
{{- end }}
